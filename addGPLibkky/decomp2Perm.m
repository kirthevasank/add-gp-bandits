function P = decomp2perm(decomp, D)
% TO BE COMPLETED
% Given a decomposition returns the corresponding permutation matrix
% d and M aren't really necessary 

  P = zeros(D);
  for  i = 1:numel(decomp)
    for j = 1:numel(decomp{i})
      P(i,j) = 
    end
  end
  
end
